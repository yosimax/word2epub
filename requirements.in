# requirements.in
# ランタイムの依存候補を列挙します。
# 必須: 実行に必要なパッケージ。オプションはコメントで説明。

# YAML 処理（必須）
PyYAML>=5.4

# テンプレート（任意だがあると Colophon のテンプレート機能が有効になる）
Jinja2>=3.0

# より堅牢な XML 処理が必要な場合（任意）
# lxml を入れると XML 操作や検証が高速かつ安定します
lxml>=4.6
